status无法正常运行的问题：
    目前采用把status的优先级提高的方式，
    或者暂停调度器：让所有其他任务在 I2C 操作期间（~176ms）无法运行。Task_Safety（10ms周期）会错过约 17 个周期，这确实不够理想
    更好的方案：改用 I2C 中断模式（HAL_I2C_Master_Transmit_IT），这样 I2C 传输由硬件+中断驱动，不依赖 CPU 忙等，任务可以随时被抢占而不影响 I2C 状态机。
    DMA + I2C 中断方式